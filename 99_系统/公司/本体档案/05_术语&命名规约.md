# 术语与命名规约

## 角色称呼
（泛指身份 vs 具体岗位名的区分规则，按本体业务补充）

## 三层视角分层
- **业务感知层**：业务用户在现场看到的
- **运营配置层**：管理员后台看到的
- **技术实现层**：开发实现层

写文档时三层各归各位，不混。

## 命名规约
- 字段名按真实数据源（xlsx / 接口 / 数据字典），不编造
- 编号体系：`CP{NN}` 产品 / `XM{NN}` 项目，编号永不复用
- 文件名以**当前状态**结尾（如 `进行中` / `已完成` / `已上线`）
